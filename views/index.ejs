
<%- include("partials/header.ejs") %>
<!DOCTYPE html>
<html lang="en">
  
<body>
    <main class="container">
      <h1 class="mt-5">🌤️ Check Your Local Weather</h1>

      <form class="weather-form mt-4" action="/submit" method="POST">
        <input
          type="text"
          name="cityname"
          class="form-control"
          placeholder="Enter your city (e.g., London)"
          required
        />
        <button type="submit" class="btn btn-primary">
          <i class="fas fa-search-location"></i> Check
        </button>
      </form>

      <div class="weather-card mt-4">
  <h4>📍 Result:</h4>

  <% if (locals.icon && locals.weatherData) { %>
    <table class="table table-bordered mt-3">
      <tr>
        <th>City</th>
        <td><%= weatherData.city %></td>
      </tr>
      <tr>
        <th>Temperature</th>
        <td><%= weatherData.temp %> °C</td>
      </tr>
      <tr>
        <th>Weather</th>
        <td><%= weatherData.description %></td>
      </tr>
      <tr>
        <th>Humidity</th>
        <td><%= weatherData.humidity %>%</td>
      </tr>
      <tr>
        <th>Rain</th>
        <td><%= weatherData.rain %></td>
      </tr>
      <tr>
        <th>Wind Speed</th>
        <td><%= weatherData.wind %> m/s</td>
      </tr>
      <tr>
        <th>Icon</th>
        <td><img src="https://openweathermap.org/img/wn/<%= icon %>.png" alt="Weather Icon"></td>
      </tr>
    </table>
  <% } else { %>
    <p><%= content %></p>
  <% } %>
</div>

    </main>

    <footer class="footer">
      <div class="container">
        <p>&copy; <%= new Date().getFullYear() %> WeatherNow | Made by Arpan</p>
        <div class="social-icons">
          <a href="https://github.com/mitrastarpan1999" target="_blank"><i class="fab fa-github"></i></a>
          <a href="https://x.com/ArpanMitra42171" target="_blank"><i class="fab fa-twitter"></i></a>
          <a href="https://www.linkedin.com/in/arpan-mitra-00033b240/" target="_blank"><i class="fab fa-linkedin"></i></a>
        </div>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>